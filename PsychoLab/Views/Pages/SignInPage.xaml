<Page x:Class="PsychoLab.Views.Pages.SignInPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PsychoLab.Views.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Вход">

    <Grid>
        <Border BorderBrush="DarkGray" BorderThickness="1" CornerRadius="5" Padding="20" Width="300" VerticalAlignment="Center" HorizontalAlignment="Center">
            <StackPanel>
                <TextBlock Text="ВХОД" HorizontalAlignment="Center" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
                <StackPanel Margin="0,10,0,20">
                    <StackPanel.Resources>
                        <Style TargetType="Label">
                            <Setter Property="Margin" Value="0,0,0,5"/>
                            <Setter Property="FontSize" Value="12"/>
                            <Setter Property="Foreground" Value="DarkGray"/>
                        </Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Height" Value="25"/>
                            <Setter Property="FontSize" Value="12"/>
                            <Setter Property="Padding" Value="5"/>
                            <Setter Property="BorderThickness" Value="1"/>
                            <Setter Property="BorderBrush" Value="DarkGray"/>
                        </Style>
                        <Style TargetType="PasswordBox">
                            <Setter Property="Height" Value="25"/>
                            <Setter Property="FontSize" Value="12"/>
                            <Setter Property="Padding" Value="5"/>
                            <Setter Property="BorderThickness" Value="1"/>
                            <Setter Property="BorderBrush" Value="DarkGray"/>
                        </Style>
                        <Style TargetType="Button">
                            <Setter Property="Height" Value="30"/>
                            <Setter Property="FontSize" Value="14"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="Background" Value="DarkGray"/>
                            <Setter Property="Margin" Value="0,5,0,0"/>
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Cursor" Value="Hand"/>
                        </Style>
                    </StackPanel.Resources>

                    <Label Content="Имя пользователя:"/>
                    <TextBox x:Name="txbUsername"/>

                    <Label Content="Пароль:"/>
                    <PasswordBox x:Name="psbPassword"/>

                    <Button Content="Войти" Click="btnSignIn_Click" x:Name="btnSignIn"/>
                    <Button Content="Отмена" Click="btnCancel_Click" x:Name="btnCancel"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Page>
